interface:
  display_name: "Memory Store"
  short_description: "跨会话、跨 Agent 存储与检索结构化记忆"
  default_prompt: "Use $memory-store under the effective memory profile; explicit is the safe default and automatic recall or storage requires opt-in."

policy:
  allow_implicit_invocation: true
